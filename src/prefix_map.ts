// Define an interface PrefixMap
// that maps strings to strings.
// Usestypescript index signature,
// map key can be any string
interface PrefixMap {
  [index: string]: string;
}

export default PrefixMap;
